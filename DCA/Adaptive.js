// CICLO FOR PARA AGREGAR EL NUMERO DE SAMPLES COMO KEYID Y SUS DESCRIPCIONES

//var samples = new Array();

con
const samplesid = "";
const description = "";
const linkids = "";
const altOUM = ""; //preguntar

for (let i = 0; i < altOUM.size(); i++) {
    samplesid += samplesid + ";";
    description += description + ";";
    linkids = "SDIGroupItem" //preguntar

}

